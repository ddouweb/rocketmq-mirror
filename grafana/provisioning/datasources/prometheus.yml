# Grafana 数据源 provisioning:启动时自动注册 Prometheus 数据源
# uid 必须是 "Prometheus",和 mqmirror-dashboard.json 里 panel 引用的 uid 一致

apiVersion: 1
datasources:
  - name: Prometheus
    uid: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
